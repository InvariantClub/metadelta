# Convert Hasura format json to Invariant Club Format JSON

This is the main library here; it computes the actual difference and emits
appropriate models that are then directly interpreted by the UI.
